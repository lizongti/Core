{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "(Windows) Make",
            "type": "shell",
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "command": "cmake --build build",
        }
    ]
}