$#include "../Service/CacheIntegratedService.hpp"

class CacheIntegratedService
{
public:
	bool Init(const std::string& json);
	bool Start();
	bool Stop();
	bool Running();
	bool RedisCommand(unsigned int id, const std::string& command_set_json);
	std::string RedisReply(unsigned int id);
};

